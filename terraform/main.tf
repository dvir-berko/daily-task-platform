terraform {
  required_version = ">= 1.5"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }

  # Uncomment after creating the bucket:
  # backend "s3" {
  #   bucket = "daily-task-platform-tfstate"
  #   key    = "prod/terraform.tfstate"
  #   region = "us-east-1"
  # }
}

provider "aws" {
  region = var.aws_region

  default_tags {
    tags = {
      Project     = "daily-task-platform"
      Environment = var.environment
      ManagedBy   = "terraform"
    }
  }
}